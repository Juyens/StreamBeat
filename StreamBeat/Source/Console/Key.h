#pragma once

#include "WindowHeader.h"

namespace sb
{
	enum class Key : int
	{
		Escape = VK_ESCAPE,
		Enter = VK_RETURN,
		Space = VK_SPACE,
		Backspace = VK_BACK,
		Delete = VK_DELETE,
		Tab = VK_TAB,
		Up = VK_UP,
		Down = VK_DOWN,
		Left = VK_LEFT,
		Right = VK_RIGHT,
	};
}